name: SkilLock

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  skil-lock:
    name: Verify skill capability deltas
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: skills-lock/skil-lock-action@6ec32f1e9a2724d6fe5752a278ae3853740ce2a0 # v0.1.2
        with:
          pin-binary: v0.1.2
